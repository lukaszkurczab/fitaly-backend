[pytest]
asyncio_mode = auto
markers =
    ai_v2: Canonical AI Chat v2 tests (`app/tests/*`).
    legacy_ai: Legacy AI/chat tests for v1 compatibility (`tests/test_ai_*` and selected v1 AI route/service tests).
